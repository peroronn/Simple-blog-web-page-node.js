<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Microblogger: Settings</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body>
    <header>
        <div class="container">
            <h1>Microblogger: Settings</h1>
            <a href="/author/home?blogID=<%= blog.BlogID %>" class="back-link">Back</a>
        </div>
    </header>
    <main>
        <section class="author-einfo">
            <h4><%= blog.BlogTitle %></h4>
            <h5><%= blog.BlogSubTitle %></h5>
            <h6><%= blog.UserName %></h6>
        </section>
        <section class="edit-article">
            <h2>Settings</h2>
            <form action="/author/update-settings" method="POST">
                <input type="hidden" name="blogID" value="<%= blog.BlogID %>">
                <div class="form-group">
                    <label for="blogTitle">Blog Title</label>
                    <input type="text" id="blogTitle" name="blogTitle" value="<%= blog.BlogTitle %>" required>
                </div>
                <div class="form-group">
                    <label for="blogSubtitle">Blog Subtitle</label>
                    <textarea id="blogSubtitle" name="blogSubtitle" rows="3" required><%= blog.BlogSubTitle %></textarea>
                </div>
                <div class="form-group">
                    <label for="userName">Author Name</label>
                    <input type="text" id="userName" name="userName" value="<%= blog.UserName %>" required>
                </div>
                <div class="form-group">
                    <label></label>
                    <button type="submit" class="update-settingbutton">Update Settings</button>
                </div>
            </form>
        </section>
    </main>
</body>
</html>