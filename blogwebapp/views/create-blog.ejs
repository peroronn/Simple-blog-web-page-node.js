<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Blog</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body>
    <header>
        <div class="container">
            <h1>Create New Blog</h1>
            <a href="/blog/home?userID=<%= user ? user.UserID : '' %>" class="back-link">Back to Home</a>
        </div>
    </header>
    <main>
        <section class="create-blog">
            <h2>New Blog Details</h2>
            <form action="/blog/create" method="POST">
                <div class="form-group">
                    <label for="blogTitle">Title</label>
                    <input type="text" id="blogTitle" name="blogTitle" required>
                </div>
                <div class="form-group">
                    <label for="blogSubTitle">Subtitle</label>
                    <textarea id="blogSubTitle" name="blogSubTitle" rows="3" required></textarea>
                </div>
                <input type="hidden" name="userID" value="<%= user ? user.UserID : '' %>">
                <div class="form-group">
                    <label></label>
                    <button type="submit" class="create-button">Create Blog</button>
                </div>
            </form>
        </section>
    </main>
</body>
</html>